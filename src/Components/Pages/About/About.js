import React from 'react';
import Loading from '../../Sheared/Loading';

const About = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default About;